import * as lib from "./lib";
import * as hasher from "./hasher";

// console.log(new hasher.hash("recognizeable").identifier);

// lib.createThread(() => {
//     console.log("is it threaded?");
// });

lib.drawQR("testingasdsadaeofhoafiejfoaifeoajfwd");

// setTimeout(() => {
//     lib.quitWindow();
// }, 3000);

setInterval(() => {}, 1000);